apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tailfront
  namespace: web
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: tailfront.37.140.243.250.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: tailfront
                port:
                  number: 80
